import { Router } from "express";

const router = Router();

// TODO: Phase 2 - File sharing routes
// router.post("/", upload.single('file'), createFilePaste);
// router.get("/:id", downloadFile);
// router.delete("/:id", deleteFile);

export default router;
