import { Router } from "express";

const router = Router();

// TODO: Phase 3 - Single-user Token Auth routes
// router.post("/login", loginHandler);
// router.post("/verify", verifyHandler);

export default router;
